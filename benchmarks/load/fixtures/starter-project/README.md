# Starter Fixture

Small deterministic Python fixture used by load harness smoke scenarios.

Known targets are described in `benchmarks/load/fixtures/metadata.json`,
including symbol search, implementation lookup, callers, tests, grep, and
watcher mutation files.
